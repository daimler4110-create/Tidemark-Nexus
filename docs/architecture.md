# Tidemark Nexus architecture

The source architecture integration guide is retained from the supplied specification. The Phase 1 implementation follows its integration spine: Auth → profiles → memberships → roles/permissions → RLS and server guards → tenant data.
